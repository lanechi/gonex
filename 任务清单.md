# `需求文档.md` 严格执行任务清单

> 只有实现、文档和测试证据同时满足时才勾选；包装器、注释或占位结构不视为完成。

## 一、Server、配置与公共 API

- [x] T01 §一：`Server` 内部状态分组，公共 API 不变。
- [x] T02 §二：使用统一 `Optional[T]` 配置对象承载值与显式设置状态，落实 Default < Config < `.env` < 环境变量 < Option。
- [x] T03 §三：将同一子系统配置收敛为 Options struct，避免重复单值 Option。
- [x] T04 §四：清理 `g` 包 aliases，保留必要入口并补回归测试。
- [x] T05 §五：Gin mode、日志 callback、quiet logging 全部经唯一 internal adapter。
- [x] T06 §六：`Engine()`、`HTTPServer()` 明确为 escape hatch 并保持兼容。

## 二、Router、Binder 与 OpenAPI

- [x] T07 §七：`router.Definition` 严格分离 metadata/runtime；文档与执行各自只读取对应视图。
- [x] T08 §八：Controller 仅识别有效 `g.Meta` 路由，错误 Action 不静默跳过。
- [x] T09 §九：反射扫描集中在注册期并缓存。
- [x] T10 §十：Binder 按 compile/path/query/header/cookie/form/file/json/default 物理拆分，核心 Binder 文件只保留编排。
- [x] T11 §十一：Binder 作为注册期编译的 binding plan，请求热路径不重复解析标签。
- [x] T12 §十二：Framework Registry 与 Gin Router 保持分离。
- [x] T13 §十三：批量路由注册保持预校验、冲突检测和失败原子性。
- [x] T14 §十四–十五：Response/Error 契约和 `ghttp.Error` 层级保持稳定。
- [x] T15 §十六：OpenAPI 物理拆分为 schema、parameters、request_body、responses、security 等实际实现文件；generator 仅负责编排。

## 三、Middleware、Session 与依赖边界

- [x] T16 §十七：Static 独立，路径边界与扩展白名单安全校验通过。
- [x] T17 §十八、二十八：Session 仅保留 context-first 协议、内存和签名 Cookie；Redis client 与任何 Redis 驱动的存储均由业务持有，`contrib/redislog` 只适配诊断日志。
- [x] T18 §十九：CORS、CSRF、Host、RequestID 作为独立 Middleware 能力实现。
- [x] T19 §二十：自动依赖边界测试覆盖核心包不得反向依赖 ghttp。
- [x] T20 §二十一：gx 保持独立 Go module，不进入运行时依赖。

## 四、gx Generator

- [x] T21 §二十二：实际建立 `gen/controller`、`gen/service`、`gen/dao`、`gen/module` pipeline 目录并迁移实现，不接受仅定义阶段名称的占位实现。
- [x] T22 §二十三：普通生成与 DAO 双目录替换使用 staging、路径安全、备份和回滚事务。
- [x] T23 §二十四：demo、basic、quick-demo、template-demo 角色和生成契约同步。

## 五、实例隔离、生命周期与版本

- [x] T24 §二十五：多 Server 路由、配置、日志、Session、模板、生命周期状态隔离。
- [x] T25 §二十六：`g.Server(name)` 与 `ghttp.NewServer()` 语义保持不同。
- [x] T26 §二十七：Lifecycle 只负责启停、后台任务跟踪和优雅退出。
- [x] T27 §二十九：补充可执行的 API freeze 语义 baseline，并递归扫描核心生产代码，阻止已删除 Redis API、Redis 驱动的 Session 依赖和旧配置标记回流。
- [x] T28 §三十：架构边界、扩展点和模块职责同步到架构文档。

## 六、验收与交付

- [x] T29：根 README、gx README、examples README、skills 与架构文档完成受影响同步。
- [x] T30：核心 test/race/vet、gx test/vet、examples test/vet、`git diff --check` 通过；gx 集成测试已在允许本地监听的环境通过。

## 七、定时任务管理

- [x] T31《添加定时任务管理》：新增独立 `scheduler` 公共契约和私有 gocron 引擎，覆盖 Cron/Every/Once、命名、Context、超时、panic recovery、Logger、`SkipIfRunning`/`AllowOverlap`/`QueueOne`；生命周期强制为 `Start(ctx)`、非阻塞 `Stop()`、`Wait(ctx)`，由 `ghttp.Server` 在 HTTP drain 后等待，并验证多 Server 隔离、配置和反向依赖。同步根 README、架构文档、demo skill 与 basic 示例；gx 不生成此运行时 API，未修改 gx 模板或生成器。

## 最终收口

T01–T30 均已完成，并分别有实现、测试或文档证据；gx 四类生成器已完成真实
pipeline 迁移，API freeze 已采用语义 baseline。

## 已执行验证

- 所有 module：各自在 module 根目录运行 `rtk proxy env GOCACHE=/private/tmp/gonex-<module>-tidy-gocache go mod tidy -diff`，均无差异。
- 核心 module：`rtk proxy env GOCACHE=/private/tmp/gonex-gocache go test ./...`、`rtk proxy env GOCACHE=/private/tmp/gonex-race-gocache go test -race ./...`、`rtk proxy env GOCACHE=/private/tmp/gonex-vet-gocache go vet ./...`。
- gx module：`rtk proxy env GOCACHE=/private/tmp/gonex-gx-gocache go test ./...`、`rtk proxy env GOCACHE=/private/tmp/gonex-gx-gocache go vet ./...`。
- `examples/demo`、`basic`、`quick-demo`、`template-demo`：各自 module 的 `rtk proxy env GOCACHE=/private/tmp/gonex-<module>-gocache go test ./...` 与 `rtk proxy env GOCACHE=/private/tmp/gonex-<module>-gocache go vet ./...`。
- `benchmarks/gx`：`rtk proxy env GOCACHE=/private/tmp/gonex-benchmarks-gocache go test ./...` 与 `rtk proxy env GOCACHE=/private/tmp/gonex-benchmarks-gocache go vet ./...`。
- 仓库根目录：`rtk git diff --check`。
