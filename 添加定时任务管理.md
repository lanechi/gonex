可以内置，但我建议 **内置“调度能力”，不要把第三方定时器的 API 直接暴露成 gonex API**。

对你这个纯 Web Framework 来说，定时任务和 `lifecycle` 是天然相关的：Server 启动时启动 scheduler，Server shutdown 时停止 scheduler；这比让业务自己额外管理 goroutine 更一致。

## 我推荐的方案

优先考虑 **`go-co-op/gocron/v2`**，而不是直接用 `robfig/cron/v3`。

`robfig/cron/v3` 很轻，核心就是 cron parser + job runner，适合你只想支持：

```go
"0 */5 * * * *"
```

这种 cron 表达式。([GitHub][1])

但 `gocron/v2` 更像完整 scheduler，目前仍在持续维护，已经提供 interval、one-time job、运行次数限制、停止时间、DST 策略、scheduler shutdown 等能力，跟框架内置模块更匹配。([GitHub][2])

我会把它设计成：

```text
gonex/
├── scheduler/
│   ├── scheduler.go
│   ├── job.go
│   ├── options.go
│   └── gocron.go
│
└── ghttp/
    └── server.go
```

核心不要让用户看到：

```go
*gocron.Scheduler
```

而是 gonex 自己定义最小接口：

```go
package scheduler

type Scheduler interface {
	Start() error
	Stop(ctx context.Context) error

	Add(Job) error
	Remove(name string) error

	Jobs() []JobInfo
}
```

任务可以定义得很简单：

```go
type Job struct {
	Name string

	Schedule Schedule

	Handler func(context.Context) error
}
```

Schedule：

```go
type Schedule interface {
	isSchedule()
}

type Cron struct {
	Expr string
}

type Every struct {
	Duration time.Duration
}
```

使用：

```go
s := ghttp.NewServer(
	ghttp.WithScheduler(scheduler.New()),
)
```

然后业务注册：

```go
s.Scheduler().Add(scheduler.Job{
	Name: "cleanup-cache",

	Schedule: scheduler.Every{
		Duration: 10 * time.Minute,
	},

	Handler: func(ctx context.Context) error {
		return cleanupCache(ctx)
	},
})
```

或者 cron：

```go
s.Scheduler().Add(scheduler.Job{
	Name: "daily-report",

	Schedule: scheduler.Cron{
		Expr: "0 0 2 * * *",
	},

	Handler: func(ctx context.Context) error {
		return generateDailyReport(ctx)
	},
})
```

## 生命周期应该这样接

这是最重要的一点。

不要：

```go
scheduler.Start()
server.Run()
```

让用户自己管理。

而是：

```text
NewServer
    ↓
register jobs

Server.Run()
    ↓
Lifecycle.Start
    ↓
Scheduler.Start
    ↓
HTTP Server Start


Server.Shutdown()
    ↓
stop accepting HTTP
    ↓
cancel application context
    ↓
Scheduler.Stop
    ↓
wait jobs
    ↓
Lifecycle shutdown hooks
```

可以直接接你已有的 `lifecycle`：

```go
func (s *Server) initScheduler() {
	s.lifecycle.OnStart(func(ctx context.Context) error {
		return s.scheduler.Start()
	})

	s.lifecycle.OnShutdown(func(ctx context.Context) error {
		return s.scheduler.Stop(ctx)
	})
}
```

这样 scheduler 不需要自己再发明一套 lifecycle。

---

## 我不建议直接做成 `ghttp` 的核心概念

也就是说，不要写成：

```go
ghttp.Cron(...)
ghttp.AddJob(...)
ghttp.Every(...)
```

因为 scheduler 本身不是 HTTP。

更合理的是：

```go
scheduler.New()
```

然后由：

```go
ghttp.Server
```

托管它的生命周期。

依赖关系：

```text
Application
     │
     ├────► scheduler
     │
     ▼
   ghttp
     │
     ▼
 lifecycle


scheduler
    │
    ▼
gocron/v2
```

而不是：

```text
scheduler → ghttp
```

`scheduler` 必须保持独立。

---

## 第三方库也不要泄漏

这是我尤其建议你注意的。

### 不要这样

```go
func (s *Server) Scheduler() gocron.Scheduler
```

也不要：

```go
func WithScheduler(opts ...gocron.SchedulerOption)
```

否则你以后想：

```text
gocron → robfig/cron
```

基本就等于 breaking change。

### 应该这样

```go
func (s *Server) Scheduler() scheduler.Scheduler
```

内部：

```go
type gocronScheduler struct {
	inner gocron.Scheduler
}
```

这跟你现在 logging 不把 Zap 类型泄漏出去是一个原则。

---

## 还应该内置这几个能力

不要一次做成 Quartz 那种重量级系统。我认为 gonex scheduler 第一版只需要：

| 能力                | 建议      |
| ----------------- | ------- |
| Cron              | 要       |
| `Every(duration)` | 要       |
| One-time          | 可以要     |
| Job name          | 要       |
| Context           | 必须      |
| Panic recovery    | 必须      |
| Logger            | 必须      |
| Graceful shutdown | 必须      |
| 防止同一任务重叠运行        | 要       |
| Run immediately   | 可以      |
| Job timeout       | 建议      |
| Retry             | 不建议核心内置 |
| Persistent jobs   | 不要      |
| Distributed lock  | 不要      |
| DB job store      | 不要      |
| Web 管理后台          | 不要      |
| Worker queue      | 不要      |

尤其是 **重入控制**。

比如一个任务每分钟执行：

```text
10:00 start
```

但是执行 90 秒。

那么 10:01 到底：

```text
A. 再启动一个
B. 跳过
C. 排队
```

必须有明确策略。

我建议默认：

```go
scheduler.WithOverlapPolicy(
	scheduler.SkipIfRunning,
)
```

另外支持：

```go
scheduler.AllowOverlap
scheduler.QueueOne
```

第一版甚至可以只提供：

```text
Allow
Skip
```

---

## Job 最好支持 middleware

这个很适合 gonex。

例如：

```go
scheduler.Use(
	scheduler.Recovery(),
	scheduler.Logging(logger),
)
```

执行链：

```text
Recovery
   ↓
Logging
   ↓
Timeout
   ↓
Job Handler
```

甚至定义：

```go
type Handler func(context.Context) error

type Middleware func(Handler) Handler
```

这和 Web middleware 思路一致，但 `scheduler` 不依赖 Gin。

以后用户可以：

```go
scheduler.Use(
	tracingMiddleware,
	metricsMiddleware,
)
```

---

## Context 要从 Server lifecycle 传进去

不要使用：

```go
context.Background()
```

启动任务。

应该：

```text
Server Context
      ↓
Scheduler Context
      ↓
Job Context
```

Server shutdown：

```go
cancel()
```

正在运行的 job 就可以收到：

```go
ctx.Done()
```

例如：

```go
func syncData(ctx context.Context) error {
	for {
		select {
		case <-ctx.Done():
			return ctx.Err()

		default:
			// work
		}
	}
}
```

这才是真正的 graceful shutdown。

---

## 日志也应该直接接 gonex Logger

比如自动输出：

```text
INFO scheduler job started
     job=sync-users

INFO scheduler job finished
     job=sync-users
     duration=823ms

ERROR scheduler job failed
      job=sync-users
      duration=1.2s
      error="database timeout"
```

不要让 gocron 自己的 logger 体系暴露出来。

---

# 我建议最终 API

用户体验可以做到：

```go
srv := g.Server()

srv.Scheduler().Add(
	scheduler.Job{
		Name: "sync-users",

		Schedule: scheduler.Every{
			Duration: 10 * time.Minute,
		},

		Handler: func(ctx context.Context) error {
			return userService.Sync(ctx)
		},
	},
)

srv.Run()
```

Cron：

```go
srv.Scheduler().Add(
	scheduler.Job{
		Name: "daily-cleanup",

		Schedule: scheduler.Cron{
			Expr: "0 0 3 * * *",
		},

		Handler: cleanup,
	},
)
```

一次执行：

```go
srv.Scheduler().Add(
	scheduler.Job{
		Name: "warm-cache",

		Schedule: scheduler.Once{
			At: time.Now().Add(time.Minute),
		},

		Handler: warmCache,
	},
)
```

配置：

```yaml
server:
  scheduler:
    enabled: true
    timezone: America/Los_Angeles
```

但是我**不建议第一版支持 YAML 声明具体业务 Job**，例如：

```yaml
jobs:
  cleanup:
    cron: xxx
```

因为配置没办法优雅表示：

```go
Handler
```

任务还是应该代码注册。

---

# 放 core 还是 contrib？

这次我反而倾向于：

> **可以直接作为 gonex core package：`scheduler/`。**

虽然内部依赖第三方 `gocron`，但 scheduler 是通用 runtime capability，而且与你已有的 lifecycle 配合非常紧密。

和 GORM 不一样：

```text
GORM
→ 数据访问技术选择

Scheduler
→ 应用 Runtime 生命周期能力
```

所以：

```text
logging
config
lifecycle
scheduler
```

可以属于同一层。

但必须满足一个条件：

> **gonex 定义自己的 Scheduler contract，gocron 只是内部 engine。**

这样未来你甚至可以：

```text
scheduler/
├── scheduler.go       // public
├── job.go             // public
├── middleware.go      // public
└── internal/
    └── gocron/
        └── scheduler.go
```

我认为这是最适合 `gonex` 的做法：**内置 scheduler 是合理的，但内置的是 gonex 的调度抽象和生命周期集成，不是把 gocron 包一层后原样暴露。** ([GitHub][2])

如果只让我选一个实现库，我现在会选 **`go-co-op/gocron/v2`**；如果你想让 gonex 极端轻量，只需要标准 cron 表达式，那才选 `robfig/cron/v3`。([GitHub][1])

[1]: https://github.com/robfig/cron/blob/master/doc.go "https://github.com/robfig/cron/blob/master/doc.go"
[2]: https://github.com/go-co-op/gocron/releases "https://github.com/go-co-op/gocron/releases"
